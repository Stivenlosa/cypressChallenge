{
  "name": "cypress-framework",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "npx cypress run",
    "mocha:merge":"npx mochawesome-merge 'cypress/results/*.json' > mochawesome.json",
    "mocha:generate":"npx marge mochawesome.json",
    "clean:reports": "rm cypress/results/* || true"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^14.0.3",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "dependencies": {
    "cypress-xpath": "^2.0.1"
  }
}
